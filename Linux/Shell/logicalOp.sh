#! /bin/sh
 if [ 18 -gt 11 -a 18 -lt 20 ] #can also be written as if if [ 18 -gt 11 ] && [ 18 lt 20 ], to use && together needa use double sq brackets, || use this for OR, -o for OR operator too
 then
   echo "okay"
fi
